export default {
  navMenuItem(state) {
    return state.navMenuItem;
  },
  scrollY(state) {
    return state.scrollY;
  },
  lastScrollY(state) {
    return state.lastScrollY;
  },
  timer(state) {
    return state.timer;
  },
  inactive(state) {
    return state.inactive;
  }
}
